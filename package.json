{
  "name": "eleventy-blog-mnml",
  "version": "1.0.0",
  "description": "A Blog Template using eleventy and terminal css.",
  "scripts": {
    "build": "env NODE_ENV=production eleventy --pathprefix '/blog-eleventy/' && npm run sass",
    "serve": "concurrently 'eleventy --serve' 'npm run sass:watch'",
    "sass": "node-sass --output  docs/styles/ styles/",
    "sass:watch": "npm run sass && npm run sass -- -w",
    "clean": "rm -rf docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Arpit Batra (arpitbatra123@gmail.com)",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "concurrently": "^5.1.0",
    "node-sass": "^4.13.1"
  },
  "dependencies": {
    "@11ty/eleventy-plugin-syntaxhighlight": "^3.0.1"
  }
}
