<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <meta name="Description" content="Arpit Batra" />
    <title>{{ title }}</title>
    <link
      href="https://fonts.googleapis.com/css?family=IBM+Plex+Sans:300,400&display=swap"
      rel="stylesheet"
    />
    <link href='http://fonts.googleapis.com/css?family=Lato:400,700' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" href="/blog-eleventy/styles/common.css" />
    <link rel="stylesheet" href="/blog-eleventy/styles/homepage.css" />
  </head>
  <body>
    <div class="container">
      <h2>notes</h2>
      {{ content }}
      <br/>
      <ul class="tags">
        {% for tag in collections.tagList %}
          {% if tag != "posts" and tag %}
            {% capture tagUrl %}/tags/{{ tag }}/{% endcapture %}
            <li><a href="{{ tagUrl | url }}"class="tag">{{ tag }}</a></li>
          {% endif %}
        {% endfor %}
      </ul>
      
      <br/>
      
      <h4 class="category">Posts</h4>
      {% assign current_filter = "posts" %}
      <ul class="posts">
      {% for post in collections.posts reversed %}
        <li class="post">
          <span class="post-title">
            {% if post.data.externalLink %}
              <a href="{{ post.data.externalLink }}" target="__blank">{{ post.data.title }}</a>
            {% else %}
              <a href="{{ post.url | url }}">{{ post.data.title }}</a>
            {% endif %}
          </span>
          <span class="post-date">
            {{ post.date | date: "%b %d %Y" | downcase }}
          </span>
        </li>
      {% endfor %}
    </ul>
    </div>
  </body>
</html>
